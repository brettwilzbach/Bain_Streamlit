streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.17.0
requests>=2.31.0
openpyxl>=3.1.0

# Bloomberg API (install separately - not on PyPI)
# Direct connection to Bloomberg Terminal - no server needed!
# Install blpapi from Bloomberg's repository:
#   pip install --index-url=https://blpapi.bloomberg.com/repository/releases/python/simple/ blpapi
# 
# Note: Requires Bloomberg Terminal running with BBComm enabled
# The app connects directly via blpapi (compatible with pandas 2.0+)
